const sumar = (a,b) => a + b,
 restar = (a,b) => a - b,
 multiplicar = (a,b) => a * b,
 dividir = (a,b) => a / b,
  calculadora = {
  sumar,
  restar,
  multiplicar,
  dividir
  }

module.exports = calculadora