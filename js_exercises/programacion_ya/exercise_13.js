// Ingresar el sueldo de una persona y si supera los 3000, mostrar mesanje que debe pagar impuestos

let sueldo = 3001

if (sueldo > 3000) {
  console.log(`El usuario gana ${sueldo} debe pagar impuestos`)
}else{
  console.log(`El usuario gana ${sueldo}, no debe de pagar impuestos`)
}